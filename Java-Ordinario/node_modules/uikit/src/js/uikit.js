import { each } from 'uikit-util';
import * as components from 'uikit/src/js/components';
import UIkit from 'uikit/src/js/uikit-core';

each(components, (component, name) => UIkit.component(name, component));

export default UIkit;
