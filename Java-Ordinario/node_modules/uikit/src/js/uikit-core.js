import { each } from 'uikit-util';
import boot from 'uikit/src/js/api/boot';
import UIkit from 'uikit/src/js/api';
import * as components from 'uikit/src/js/core';

// register components
each(components, (component, name) => UIkit.component(name, component));

boot(UIkit);

export default UIkit;
